cd

cd eit62-tms/src

git pull

rm -rf /home/pi/webserver

cp -rf ./webserver /home/pi/webserver

cp -rf ../Entwurf/tms-schema.sql /home/pi/webserver/tms-schema.sql

rm -rf /home/pi/webserver/package-lock.json

cd /home/pi/webserver

cp /home/pi/.env .env

npm i

npm start
